spring:
  application:
    name: ag-gate
  cloud:
    config:
      discovery:
        enabled: true
        service-id: AG-CONFIG
      profile: dev
  rabbitmq:
    host: 106.12.151.12
    port: 5672
    username: guest
    password: guest

server:
  port: 8765

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


