spring:
  cloud:
    config:
      discovery:
        service-id: ag-config
        enabled: true
      label: master
      name: uc
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://qoocar:qoocar@localhost:8761/eureka
